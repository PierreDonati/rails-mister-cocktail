
<h1>New dose</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id %>
  <%= f.input :cocktail_id %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>

<%= link_to "Come back to cocktail", cocktail_path(@cocktail) %>

